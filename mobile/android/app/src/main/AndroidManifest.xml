<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Fase 6.2 (app do motoboy): GPS FOREGROUND durante turno online com
         entrega ativa. Sem ACCESS_BACKGROUND_LOCATION de proposito (LGPD). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Auto-update in-app (fora da Play Store): permite ao app disparar o instalador
         do sistema para o APK baixado. Android 8+ ainda pede ao usuario liberar
         "instalar apps de fontes desconhecidas" para o MenuFlow na 1a vez.
         O content:// exigido pelo Android 7+ e servido pelo FileProvider que o
         react-native-blob-util ja registra (authority "${applicationId}.provider"),
         por isso nao declaramos um provider proprio aqui. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
